import 'package:flutter/material.dart';

class Pallete {
  static const Color backgroundColor = Color.fromRGBO(245, 245, 245, 1);
  // static const Color primary = Color(0xFF69F0AE);
  static const Color primary = Colors.blueAccent;
  static const Color gradient2 = Color(0xFF69EFF0);
  static const Color gradient3 = Color(0xFF69F06A);
  static const Color borderColor = Color.fromRGBO(200, 200, 200, 1);
  static const Color whiteColor = Colors.white;
  static const Color hintTextColor = Color.fromRGBO(0, 0, 0, 0.5);
}

const Color kMainFontColor = Color.fromRGBO(0, 0, 0, 1);